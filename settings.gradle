pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        maven {
            url = uri("https://maven.pkg.github.com/tachiyomiorg/tachiyomi")
            credentials {
                username = System.getenv("AhmedHero55") ?: ""
                password = System.getenv("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") ?: ""
            }
        }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        maven {
            url = uri("https://maven.pkg.github.com/tachiyomiorg/tachiyomi")
            credentials {
                username = System.getenv("AhmedHero55") ?: ""
                password = System.getenv("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") ?: ""
            }
        }
    }
}
